# Server Configuration
server.port=8080

# CORS (Cross-Origin Resource Sharing) configuration
# This allows your React frontend (running on a different port, e.g., 3000) to connect
app.cors.allowed-origins=http://localhost:3000